'use client';

import { Container, Typography } from '@/client/components/ui';
import React from 'react';

export const IndexPage: React.FC = () => {
  return (
    <Container>
      <Typography variant="h2">Hello, world!</Typography>
    </Container>
  );
};
